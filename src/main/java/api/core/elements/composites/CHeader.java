package api.core.elements.composites;

import api.core.elements.Element;

public class CHeader extends Composite {

    public CHeader(Element...elements) {
        super("thead", elements);
    }

}
